plugins {
    id 'com.android.application'
}

android {
    namespace 'org.cigaes.binaural_player'
    compileSdk 28

    defaultConfig {
        applicationId 'org.cigaes.binaural_player'
        minSdk 23
        targetSdk 28
        versionCode 1
        versionName '1.0'

        ndk {
            abiFilters 'arm64-v8a'
        }
    }

    externalNativeBuild {
        cmake {
            path 'src/main/cpp/CMakeLists.txt'
        }
    }
}